const primerNumber = {
  properties: {
    higherNumber: {
      message: 'higherNumber must be only number and is required',
      type: 'number',
      required: true
    }
  }
}

module.exports = primerNumber
